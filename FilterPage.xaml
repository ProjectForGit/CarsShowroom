<Page x:Class="CarsShowroom.FilterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarsShowroom"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Background="White"
      Title="FilterPage">

    <Grid>
        <TextBlock TextAlignment="Center" VerticalAlignment="Center">Фильтрация</TextBlock>
        <CheckBox Content="Должность" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,100,400,0"></CheckBox>
        <CheckBox Content="Должность" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,100,400,0"></CheckBox>

        <Button x:Name="OK" Click="OK_Click" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,500,0,0" Width="100" Height="50">Применить</Button>
    </Grid>
</Page>
